<!--
Template: one instantiated wave prompt (goes inside execute.md §7, in a ```text block).
Rules when instantiating:
- Every {{placeholder}} filled with REAL data from the analysis: exact task IDs, exact
  commands that exist in that repo, exact paths. Never invent.
- The six core sections are mandatory, in this order; `# VISUAL CHECK` is a mandatory
  7th section between LOOP and WORKFLOW on UI waves — omit it otherwise. SCOPE embeds
  the Step 3 investigation (the executing session must never re-derive file lists).
  WORKFLOW is ALWAYS present — even if the executing session has no ultracode enabled,
  the prompt itself instructs the delegation — and it opens with the fixed AGENT
  BUDGET & ROLES block, followed by one `## Executor brief` per task pre-filled from
  SCOPE (FILES, DOD-SLICE, FORBIDDEN) with placeholders for what the orchestrator
  completes in session (DELIVERABLE, DESIGN DECISIONS, STOP IF). Never emit a
  WORKFLOW where the main session implements first, where an executor
  self-approves, where the diff is audited by the executor's own tier, or whose
  agents add up to more than 20 (investigators + executors + reviewers + verifier).
  Executors are `sonnet` on NO-gate waves and `opus` on ⚠gate waves; auditors are
  `opus` until the quota of 10 (1 reserved for the gate verifier), then `sonnet`;
  `model` always explicit.
- SCOPE budget: ~15 lines per task (hard cap 25 for legitimately multi-file tasks),
  ≤60 per wave. Overflow (long symbol notes, full consumer lists) stays in the task's
  Scope manifest card in detailed-plan.md; the prompt keeps files + census. Compress
  with brace-globs (`bold/{A,B,C}.tsx`) before dropping content.
- Write for Fable at high effort: dense, precise, zero ambiguity, no filler. Must
  still be executable by any model.
- Generate in the user's language; keep ALL section names in English (incl. VISUAL CHECK).
-->
Start a clean session in EXECUTION mode (auto-accept edits — e.g. `claude
--permission-mode acceptEdits`). Do NOT enter plan mode: the plan IS this prompt
(# SCOPE below); re-planning would duplicate the investigation this prompt already
carries. The only re-planning trigger is a diverging census (see # SCOPE).
Load the skill `{{project_context_skill}}`, read
`{{docs_dir}}/execute.md` (this protocol). {{Wave-specific preamble: extra skills to
load, repos to open, state assumptions — e.g. "W2 must be merged" — and what to do if
an assumption fails.}}

# GOAL — Wave {{Wx}}: {{task_titles}}
Work EXACTLY the tasks {{IDs}} (specs: `{{docs_dir}}/detailed-plan.md` §{{section}} —
read them in full; they are the specification). {{Mandatory internal order if any,
e.g. "T-W2-1 closes before T-W2-3 starts (shared schema)."}}
DoD-auto (the loop's EXIT criterion; everything by command):
- [ ] {{command}} → {{expected_result}}
- [ ] {{command}} → {{expected_result}}
{{If the wave touches UI, also: "- [ ] reviewed screenshot of the rendered page (see VISUAL CHECK)".}}
{{One block per internal task if it helps, plus an "Integration (whole wave)" block
with the repo-wide quality commands.}}

# TASKS
Create one task per ID (TaskCreate): {{IDs}}. Mark each in_progress on entry and
completed when its slice of the DoD-auto is verified. All work on the wave's single
branch (`{{branch_name}}`), checkpoint commit (Conventional, footer `Refs: <ID>`) at
each internal task close.

# SCOPE (manifest)
The investigation already ran — do NOT re-derive these lists with exploratory
Glob/Grep. Run each Census FIRST; if it diverges beyond tolerance (or a named file is
missing), STOP: re-scope against `{{docs_dir}}/detailed-plan.md` §{{section}}, update
that manifest, and report the drift in the logbook at close.
## {{ID}}
- Modify: `{{path}}` — {{1-line why: what changes there}} {{· one line per file}}
- Create: `{{path}}` — {{what it is}}
- Read first: `{{exemplar_path}}` — {{the pattern it demonstrates}}
- Impact: `{{shared_surface}}` ← {{N}} consumers ({{consumer paths or "list:
  detailed-plan §{{section}}"}}). Keep its contract for ALL of them or their
  adaptation is explicitly part of this task; DoD-auto must cover the consumers
  (build/tests of the affected apps), not just the edited files.
- Census: `{{command}}` → expected {{N}} (as of {{analysis_date}}) · tolerance
  {{exact | ±1 for small counts (<10); ±X% only for large censuses}}
- {{Symbol notes if they fit the budget — exact members to delete/preserve/rename —
  else "symbol detail: detailed-plan.md §{{section}}"}}

# LOOP
brief → execute (sub-agent) → verify (YOU run the DoD-slice) → fix → repeat. Max 5
rounds. Escalation ladder: the executor's 2nd failure of the SAME DoD line moves the
task one tier up as a NEW agent carrying the attempt log ({{sonnet → opus → you |
opus → you on ⚠gate waves}}); 3 attempts on the SAME failure at session level
without converging → stop and report (don't degrade tests, don't mark done what
isn't done). {{Wave-specific failure policy: known flaky checks, retries that don't
count as failures, when to stop early and leave DoD-human.}}

# VISUAL CHECK (mandatory if the wave touches UI: .pug/.html/.scss/.css/components)
grep + build only prove the code is THERE, not that it LOOKS right — they never catch real width,
wrong-language text, or overflow/overlap. Before marking a UI task completed, render the page in headless
Chrome and review the screenshot YOURSELF:
- Render against a local server (full env) or, if it can't boot, the deployed URL.
- Screenshot with a headless-Chrome helper (Playwright/Puppeteer + the system browser, e.g.
  `node {{repo}}/scripts/shot.{{cjs|mjs — match the repo's module system}} <url> <out.png> [lang] [width]`). Capture desktop (≥1280) AND mobile (375);
  both languages if the app is i18n. {{If the project has no screenshot helper, the FIRST UI task creates one.}}
- Read the PNG and verify: width/responsive (not "compacted"), i18n correct, no overlaps, design-system
  fonts/colors. Only then mark the UI task completed.

# WORKFLOW (fan-out)
AGENT BUDGET & ROLES: max 20 sub-agents in this session TOTAL, one counter starting
at zero, `model` always explicit. YOU (the main session) ORCHESTRATE: you decide
design and scope, audit what investigators DELIVER (solution · files ·
considerations · open questions — never code) against this SCOPE, write one
executor brief per task, run every DoD-slice and the full DoD-auto YOURSELF, and
integrate. You do NOT implement first, and you never audit a diff with a sub-agent
of your own tier. `opus` sub-agents investigate, review regression and verify (quota
10, 1 reserved for the ⚠gate verifier; audits pass to `sonnet` once spent).
`sonnet` sub-agents IMPLEMENT NO-gate tasks from your briefs — ≤4 in parallel with
EXCLUSIVE ownership of disjoint files, each in its own worktree — and return diff
summary + DoD-slice commands with output + doubts, never a bare "done". Follow-ups
to a live agent (SendMessage) cost 0; a new agent costs 1. In ultracode: Workflow
scripts per phase (investigate → implement → review) with ≤20 agent() calls in
total, `isolation: 'worktree'` on parallel executors, no unbounded loops or panels.
The 21st agent is a protocol violation — stop and ask. Log `agents used: n/20 (opus
a · sonnet b) · escalations` at close.
{{ALWAYS spell out the cycle with real IDs and scopes, e.g.:
1. Investigate: launch {{k}} `opus` sub-agents IN PARALLEL, one per {{scope}}, each
   returning solution · files to touch (with why) · considerations · open questions
   — NOT code. Sub-agents do NOT run {{expensive_global_check}}.
2. Audit their deliverables against SCOPE (consumers covered? files missing?
   inviolable decisions respected?); iterate with SendMessage, ≤5 rounds.
3. Brief: complete the `## Executor brief` blocks below (DELIVERABLE · DESIGN
   DECISIONS · STOP IF) from the audited deliverables.
4. Execute: launch {{k ≤ 4}} `sonnet` executors IN PARALLEL, one brief each, disjoint
   files ({{A: scope}}, {{B: scope}}), own worktrees; their gate is their DOD-SLICE.
5. Verify: run each DOD-SLICE yourself; a failure goes back to the SAME executor;
   the 2nd failure of the same line escalates (sonnet → opus → you) with the
   attempt log. Integrate the worktrees.
6. Review: {{r}} `opus` sub-agents (`sonnet` once the quota is spent) review
   regression over the integrated diff + the named flows; every finding goes back
   to the executor holding the task; then run the FULL DoD-auto yourself.}}
{{If ⚠gate: "This is a ⚠gate wave: the executor in step 4 is ONE `opus` sub-agent
(serial, not sonnet; ladder opus → you). Before closing: MANDATORY adversarial
verification — launch ANOTHER `opus` sub-agent (the reserved one) that did NOT see
the implementation, give it only the diff + the DoD + the attack checklist for
{{Wx}} in execute.md §6. Every finding is fixed and re-verified before close."}}
{{If advisory (NO-gate but the task spec or risk profile asks for an independent
review): "Wave is NO-gate, with ADVISORY review — launch ONE `opus` sub-agent
(`sonnet` if the quota is spent) that did NOT see the implementation, give it the
diff + the DoD + the advisory checklist in execute.md §6. Findings are fixed before
close but do not block delivery policy."}}
{{If NO-gate plain: "Wave is NO-gate (§5): no adversarial verification required."}}

## Executor brief — {{ID}} ({{one block per task; ≤40 lines each}})
- TASK: {{ID}} — {{title}} · executor: {{sonnet | opus (⚠gate)}}
- FILES: {{Modify/Create with why + Read-first exemplars — copied from # SCOPE}}
- DELIVERABLE: {{filled in session: Opus's audited deliverable with your decisions}}
- DESIGN DECISIONS: {{filled in session: what is settled and not negotiable}}
- DOD-SLICE: {{command}} → {{expected}} {{· the lines of the DoD-auto this task owns}}
- STOP IF: {{an expected file is missing · the census diverges · a consumer contract
  would change · a test would have to be degraded · {{task-specific condition}}}} —
  stop and report, do not improvise.
- FORBIDDEN: {{paths outside this task}} · no dependency upgrades · no secrets
- RETURN: diff summary per file · every DOD-SLICE command with its output · open
  questions. Never report "done" — the orchestrator verifies.

# GUARDRAILS & CLOSE
Touch nothing outside the scope of {{IDs}} ({{explicit allowed paths}}). No secrets in
code/commits/logs. Agent budget 20 with fixed roles — you orchestrate and verify,
opus investigates/audits (quota 10), sonnet implements from briefs, ladder sonnet →
opus → you — is a hard limit of this session (see WORKFLOW).
{{Wave-specific hard limits: "do NOT upgrade dependency X", "do NOT
touch folder Y (Wave 3's scope)", inviolable decisions from master-plan §8 that this
wave could violate.}} When the DoD-auto is green: {{delivery per execute.md §3 —
open MR / commit without push}}, then run the FULL doc-sync at close (`execute.md`
§2b — "append the logbook" means ALL of it): logbook entry in `{{docs_dir}}/task.md`
(TODAY's date — never hardcoded — + branch/MR + tasks closed + **files touched +
manifest drift** + notes + wall clock + **agents used n/20 (opus a · sonnet b)**,
one line per agent with its model, role and scope + **escalations** (none, or
ID: tier → tier with the reason)), status flip in task.md's wave map AND this
prompt's `execute.md §7` header (✅ / ⛔ if blocked on a DoD-human), mark {{IDs}} ✅ in
`detailed-plan.md` and update THEIR Scope manifests with what was ACTUALLY touched,
refresh the Scope manifests of LATER waves whose surfaces this wave changed (and
regenerate their §7 prompts), and delta-refresh the project context skill for the
files touched. The doc-sync ENDS with `git -C {{docs_dir}} add -A && git commit -m
"docs({{wave}}): …" && git push` — `{{docs_dir}}` is its own NESTED git repo (the
harness reports the container as "not a git repo": don't believe it); an unpushed
logbook does not exist for the next session.
END the delivery message with a LINKS BLOCK, no filler — in this order: (a) the
wave's viewable result if it renders, ALWAYS AS TWO LINES: `localhost` FIRST
({{owner}} validates from iTerm on the Mac) and the LAN IP after it (so it also opens
from a phone). Never one without the other — the LAN IP changes with the network and
arrives dead, `localhost` does not. Read the IP at close time, bind to `0.0.0.0` and
`curl` both before handing them over; start the preview server yourself if none runs,
with HTTP Range support or mobile video dies; served PDF; report); (b) ALWAYS the
project's canonical artifacts from `{{docs_dir}}/artifacts.json` — republished to
their SAME URL first if their HTML changed (Artifact tool with `url`, read the live
version before publishing; a flipped badge without a republish is a stale link);
(c) repo paths and commit hashes, one line each — reference, not delivery.
FORMAT: every link is a plain Markdown bullet `- [emoji Title](url)` — never inside
a code fence, backticks, or 4-space indentation ({{owner}} taps these on a phone;
code-formatted links are dead text). The raw URL inside the Markdown link keeps
Claude Code's quick-access footer badges working too. MEDIA AT THE END: send
screenshots/photos/videos (SendUserFile) at the very end of the close — narrative
first, then the files, then a SHORT final message that is just the links block, so
{{owner}} sees media + links on a phone without scrolling back up. Then
list the pending DoD-human marking those tasks blocked until {{owner}} confirms:
- {{ID}}: {{human_only_actions, or "none → completed when its DoD-auto passes"}}
