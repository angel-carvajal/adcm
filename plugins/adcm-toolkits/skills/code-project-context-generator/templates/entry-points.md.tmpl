# {{PROJECT_NAME}} — Entry Points and Main Flows

Load it when you need to know how the project starts, what command to run, what the main routes are, or how a specific flow is triggered.

---

## Commands

### Development
```bash
{{DEV_COMMANDS}}
```

### Build
```bash
{{BUILD_COMMANDS}}
```

### Tests
```bash
{{TEST_COMMANDS}}
```

### Lint/Format
```bash
{{LINT_COMMANDS}}
```

### Migrations / DB
```bash
{{DB_COMMANDS}}
```

### Other relevant scripts
```bash
{{OTHER_SCRIPTS}}
```

---

## Startup files

{{MAIN_FILES}}

List of the `main.py`, `index.ts`, `server.js`, `artisan`, `manage.py`, `Dockerfile CMD`, etc. with a description of what they start.

---

## Main routes

{{ROUTES_MAP}}

- `GET /api/...` — [description] → handler at [path]
- `POST /...` — [description]
- CLI command: `[cmd]` — [description]

---

## Key business flows

{{KEY_FLOWS}}

Document the 3-5 most important flows of the project from start to finish:
- [Flow 1]: [trigger] → [modules involved] → [result]
- [Flow 2]: ...

---

## Jobs / Queues / Workers

{{BACKGROUND_JOBS}}

Background jobs, queues, cron jobs, async workers.

---

## External integrations

{{INTEGRATIONS}}

Third-party APIs, incoming webhooks, outgoing webhooks, cloud services.
