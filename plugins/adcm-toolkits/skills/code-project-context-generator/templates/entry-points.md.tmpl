# {{PROJECT_NAME}} — Entry Points y Flujos Principales

Cárgalo cuando necesites saber cómo arranca el proyecto, qué comando correr, cuáles son las rutas principales o cómo se dispara un flujo específico.

---

## Comandos

### Desarrollo
```bash
{{DEV_COMMANDS}}
```

### Build
```bash
{{BUILD_COMMANDS}}
```

### Tests
```bash
{{TEST_COMMANDS}}
```

### Lint/Format
```bash
{{LINT_COMMANDS}}
```

### Migraciones / DB
```bash
{{DB_COMMANDS}}
```

### Otros scripts relevantes
```bash
{{OTHER_SCRIPTS}}
```

---

## Archivos de arranque

{{MAIN_FILES}}

Lista de los `main.py`, `index.ts`, `server.js`, `artisan`, `manage.py`, `Dockerfile CMD`, etc. con una descripción de qué arrancan.

---

## Rutas principales

{{ROUTES_MAP}}

- `GET /api/...` — [descripción] → handler en [path]
- `POST /...` — [descripción]
- CLI command: `[cmd]` — [descripción]

---

## Flujos de negocio clave

{{KEY_FLOWS}}

Documenta los 3-5 flujos más importantes del proyecto de principio a fin:
- [Flujo 1]: [trigger] → [módulos involucrados] → [resultado]
- [Flujo 2]: ...

---

## Jobs / Queues / Workers

{{BACKGROUND_JOBS}}

Trabajos en background, queues, cron jobs, workers asíncronos.

---

## Integraciones externas

{{INTEGRATIONS}}

APIs de terceros, webhooks entrantes, webhooks salientes, servicios cloud.
