# {{PROJECT_NAME}} — Detailed Architecture

This file contains the full project tree with a 1-2 line description per folder. Load it only when you need to see beyond the high-level map in the SKILL.md.

---

## Full tree

```
{{FULL_TREE}}
```

---

## Per-folder description (breadth-first)

{{FOLDER_DESCRIPTIONS}}

Format for each entry:

### `path/to/folder/`
**Purpose:** [what lives here]
**Predominant types:** [.ts, .tsx | .php | .py]
**Size:** [files / KB]
**Notes:** [detected patterns, entry points, critical modules]
**Full detail:** `folders/path-to-folder.md` (if applicable)

---

## Dependencies between layers

{{LAYER_DEPENDENCIES}}

Describe how the dependency flows: controllers → services → repositories → models, for example. If the project has multiple layers or modules that depend on each other, document the flow here.

---

## Canonical paths (cheat-sheet)

{{CANONICAL_PATHS}}

Shortcuts for finding common files:
- Auth → `[path]`
- DB models → `[path]`
- Per-environment config → `[path]`
- Tests → `[path]`
- Migration scripts → `[path]`
