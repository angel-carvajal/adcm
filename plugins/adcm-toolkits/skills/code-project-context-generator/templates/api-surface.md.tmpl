<!-- auto-generated: scan {{SCAN_DATE}} — do not edit; refresh by re-running code-project-context-generator -->

# {{PROJECT_NAME}} — API Surface

Load it when you need to know which endpoints/routes exist, their HTTP methods, or where each one is defined. Derived from a scan — verify against the real route files before relying on an exact path.

---

## Style

{{API_STYLE}}

(`rest` / `graphql` / `mixed` / none detected)

---

## Endpoints

{{API_ENDPOINTS_TABLE}}

| Method | Path | Defined in |
|--------|------|------------|
| ...    | ...  | ...        |

> `RESOURCE` = a Laravel `apiResource`/`resource` (expands to the standard CRUD verbs).
> `ANY` = method not pinned by the scanner (e.g. Flask `@app.route` without explicit methods).
> Capped at 150 endpoints; if truncated, see the route files listed under "Defined in".

---

## OpenAPI / GraphQL specs

{{API_SPECS}}

Spec files found in the repo (`openapi.json`, `swagger.yaml`, `*.graphql`, …). Read these for the authoritative contract.

<!-- end auto-generated -->
